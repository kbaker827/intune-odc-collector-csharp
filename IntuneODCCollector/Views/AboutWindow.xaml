<Window x:Class="IntuneODCCollector.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Intune ODC Collector"
        Width="420" Height="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Icon="pack://application:,,,/app.ico">

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- App name + icon -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
            <Image x:Name="AppIcon"
                   Width="48" Height="48"
                   Margin="0,0,14,0" VerticalAlignment="Center"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Intune ODC Collector"
                           FontFamily="Segoe UI" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock x:Name="VersionText"
                           FontFamily="Segoe UI" FontSize="10" Foreground="#555"/>
            </StackPanel>
        </StackPanel>

        <!-- Divider -->
        <Separator Grid.Row="1" Margin="0,4,0,12"/>

        <!-- Description -->
        <TextBlock Grid.Row="2"
                   FontFamily="Segoe UI" FontSize="10" TextWrapping="Wrap"
                   Foreground="#333" LineHeight="18">
            Collects Intune One Data Collector (ODC) diagnostic logs for Microsoft Support.
            Supports native C# collection and the official Microsoft PowerShell script.<LineBreak/><LineBreak/>
            Rewritten from Python as a self-contained Windows application — no Python or
            .NET runtime required.
        </TextBlock>

        <!-- Spacer -->
        <Grid Grid.Row="3"/>

        <!-- Divider -->
        <Separator Grid.Row="4" Margin="0,0,0,10"/>

        <!-- Footer: repo link + close button -->
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock FontFamily="Segoe UI" FontSize="10" Foreground="#555"
                           VerticalAlignment="Center" Margin="0,0,4,0" Text="Source:"/>
                <TextBlock FontFamily="Segoe UI" FontSize="10" VerticalAlignment="Center">
                    <Hyperlink x:Name="RepoLink" RequestNavigate="RepoLink_RequestNavigate">
                        github.com/kbaker827/intune-odc-collector-csharp
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Close" IsDefault="True" IsCancel="True"
                    Width="75" Padding="0,5"
                    FontFamily="Segoe UI" FontSize="10"
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>
